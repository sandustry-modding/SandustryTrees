/** One solid row wide enough to catch a small sand pile. */
export const SIEVE_WIDTH = 4;

/** Footprint height in cells (top row only). */
export const SIEVE_HEIGHT = 1;

/** One sand grain every two seconds per sieve. */
export const SIEVE_PROCESS_INTERVAL_MS = 2000;

/** Roughly one pine cone per 100 sand grains on average. */
export const SIEVE_PINE_CONE_CHANCE = 0.01;
